[project]
name = "log-analyzer-platform"
version = "0.2.0"
description = "Enterprise Log Analyzer with CLI, Backend API, and React Dashboard"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["cli/tests", "backend/tests"]
pythonpath = ["cli/src", "backend/src"]
