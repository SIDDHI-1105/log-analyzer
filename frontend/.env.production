# Production environment variables for Render
# The actual API URL will be injected at build time or configured in the app

# Note: The frontend uses relative paths (/api/v1) which works with the proxy
# If deploying frontend separately on Render Static Site, update the API base URL
VITE_API_BASE_URL=https://log-analyzer-backend.onrender.com/api/v1
